rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write to all collections for authenticated or application users
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
